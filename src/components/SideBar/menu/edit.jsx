import React from 'react';

import { SideBarButton, SideBarContent } from '..';

export const Button = props => <SideBarButton {...props} icon="pencil"/>;

export const Content = () => <SideBarContent>
    TODO
</SideBarContent>;
